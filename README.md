# Syracuse Low-Stress Bike Router

A static web app that finds the least-stressful cycling route between two points in Syracuse, NY. Routing runs entirely in the browser using a Rust navigation engine compiled to WebAssembly — no server required.

**Live:** [https//syrbikemaps.com](https//syrbikemaps.com)

---

## Features

- **Low-stress routing** — Dijkstra's algorithm weighted by Level of Traffic Stress (LTS), strongly preferring paths, trails, and protected bike lanes over unprotected roads
- **Hill avoidance slider** — fitness-level slider (Casual → Athletic) that scales hill penalties from full avoidance down to none
- **Stress overlay** — full street network coloured by LTS level (green / yellow / red)
- **Turn-by-turn directions** — step list with bearing-based turn detection; tap any step to pan the map
- **Route stats** — distance, estimated time (10 mph), LTS breakdown, max grade
- **Address search** — geocoding via Photon/Komoot with keyboard navigation
- **Export to PDF** — prints route, stats, and directions with a canvas snapshot of the map
- **Mobile-friendly** — full-screen map with a slide-out drawer on small screens
- **Fully offline** — all assets (WASM, map tiles, fonts, glyphs) are local; only geocoding requires internet

---

## Stack

| Layer | Technology |
|-------|-----------|
| Routing engine | Rust → WebAssembly (`wasm-pack`, `wasm-bindgen`) |
| Map | MapLibre GL JS v5.22.0 (local) + PMTiles vector tiles |
| Tile data | OpenMapTiles schema, MBTiles → PMTiles via `pmtiles convert` |
| Geocoding | Photon (forward) + Nominatim (reverse) |
| Data pipeline | Python 3.13 + DuckDB |
| Elevation | OpenTopoData SRTM30m API |
| Street data | PeopleForBikes BNA, January 2025 |

---

## Project Structure

```
syr-bike-navigation/
├── index.html              # entire front-end (single file)
├── serve.py                # local dev server with HTTP Range support (needed for PMTiles)
├── syr_map.json            # compiled graph + ways (~2 MB, generated)
├── syr_map.duckdb          # intermediate database (generated)
├── pkg/                    # compiled WASM output (generated by wasm-pack)
│   ├── nav_wasm.js
│   ├── nav_wasm_bg.wasm
│   └── nav_wasm.d.ts
├── nav_wasm/               # Rust navigation engine
│   ├── src/lib.rs
│   └── Cargo.toml
├── db_loader/              # Python data pipeline
│   ├── load.py             # extract graph from HTML → DuckDB
│   ├── fetch_elevations.py # SRTM elevation → per-edge grade
│   ├── export_json.py      # DuckDB → syr_map.json
│   └── pyproject.toml
├── tiling/
│   └── syracuse.pmtiles    # vector map tiles (converted from MBTiles)
├── lib/
│   ├── maplibre-gl.js      # MapLibre GL JS v5.22.0
│   ├── maplibre-gl.css
│   ├── pmtiles.js          # PMTiles protocol handler v4.3.0
│   ├── glyphs/
│   │   └── Open Sans Regular/  # 256 glyph PBF files for map labels
│   └── fonts/              # DM Mono + Syne woff2 files (unused after light theme)
└── DOCS.md                 # full technical documentation
```

---

## Getting Started

### Prerequisites

- [Rust](https://rustup.rs/) + `wasm-pack` (`cargo install wasm-pack`)
- [Python 3.13+](https://python.org) + [uv](https://github.com/astral-sh/uv)

### 1. Build the WASM module

```bash
cd nav_wasm
wasm-pack build --target web --release
```

Output lands in `pkg/`.

### 2. Run the data pipeline

Only needed if you want to regenerate `syr_map.json` (e.g. after changing the source data or re-fetching elevations).

```bash
cd db_loader
uv sync
uv run python load.py               # parse graph → DuckDB
uv run python fetch_elevations.py   # fetch SRTM elevations (takes ~3 min)
uv run python export_json.py        # export DuckDB → syr_map.json
```

### 3. Serve locally

Use `serve.py` instead of Python's built-in server — PMTiles requires HTTP Range requests which the built-in server doesn't support.

```bash
python serve.py
# open http://localhost:8080
```

---

## How It Works

See [DOCS.md](DOCS.md) for a full technical breakdown covering:

- How the street graph and elevation data are loaded into DuckDB
- How per-edge grade (rise/run) is computed from SRTM elevation
- Dijkstra's algorithm implementation in Rust — min-heap, lazy deletion, hill factor
- How `wasm-pack` compiles the crate and what the JS glue layer does
- The browser loading sequence (`Promise.all` over WASM init + JSON fetch + MapLibre load)
- Map rendering: MapLibre GL JS, PMTiles protocol, local glyph files

---

## Data

Street network and LTS classifications from [PeopleForBikes BNA](https://bna.peopleforbikes.org/) (January 2025).
Elevation from [OpenTopoData](https://www.opentopodata.org/) SRTM30m via the public API.
Map tiles from [MapTiler](https://www.maptiler.com/) OpenMapTiles schema (2020), converted to PMTiles.

LTS scale: **1** = comfortable (trails, paths, calm streets) · **2** = moderate (bike lanes) · **3** = stressful (no infrastructure)

Routing cost: `length × LTS_penalty × hill_penalty(grade) × hill_factor` where LTS penalties are 1×, 2×, 10×; hill penalties range from 1× (≤4%) to 50× (>12%); and `hill_factor` (0.0–1.0) is set by the fitness slider.
